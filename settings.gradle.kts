rootProject.name = "ref-domain-customer-service"

pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            url = uri("https://idigital.jfrog.io/artifactory/default-maven-local")
            credentials {
                username = "github-robot"
                password = "&WaRaF+F(3(hW3H3"
            }
        }
    }
}